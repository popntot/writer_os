# Parallel stream prompts — Writer OS

Generated by `/parallel-streams-v2`. Source of truth: this file lists what's in flight; the per-stream prompt files in `docs/streams/` are the actual worker instructions; the dispatch log in `docs/streams/_dispatch-log.md` is append-only history.

## Iteration 1 — 2026-05-20 (base `293ece9`)

### Stream A — DS-1 design system foundation `[codex-able]` ✅ merged `213ebb2`

- **Issue:** [#42](https://github.com/popntot/writer_os/issues/42)
- **Worker:** `task-mpegjruc-x7jipy` (24m 43s)
- **Prompt:** [`docs/streams/stream-a-ds1-foundation.prompt.md`](streams/stream-a-ds1-foundation.prompt.md)
- **Handoff:** [`docs/handoffs/stream-a-ds1-foundation.md`](handoffs/stream-a-ds1-foundation.md)
- **Scope (landed):** Locked `docs/interfaces/design-system.md`. 12 SwiftUI primitives + Tokens.swift + `WriterFade` (reduce-motion-aware) under `apps/ios/WriterOS/DesignSystem/`. Snapshot tests in light + night (24 reference PNGs). AGENTS.md UI section replaced. No existing iOS views modified.
- **Unblocks:** DS-2/3/4/5 (next iteration).

### Stream B — Real LLM triage + sweeps cron `[codex-able]` ✅ merged `e502193`

- **Issue:** [#12](https://github.com/popntot/writer_os/issues/12)
- **Worker:** `task-mpegjwzm-0jxnhw` (10m 4s) + resume `task-mpeibk7h-dsvlhu` (2m 5s, tryEmbed type narrowing fix)
- **Prompt:** [`docs/streams/stream-b-real-llm-triage.prompt.md`](streams/stream-b-real-llm-triage.prompt.md)
- **Handoff:** [`docs/handoffs/stream-b-real-llm-triage.md`](handoffs/stream-b-real-llm-triage.md)
- **Scope (landed):** Stub triage replaced with Claude-driven two-tier classification (auto-file ≥ 0.80 / propose / no-match < 0.50). Embedding-similarity candidate retrieval over `embeddings` with PGlite text-fallback. Hourly Worker cron (`crons = ["0 * * * *"]`) runs both `runAuditWindowSweep` + `runStaleSweep`. Tunable thresholds via `WRITER_OS_TRIAGE_HIGH_CONFIDENCE` / `WRITER_OS_TRIAGE_LOW_CONFIDENCE`. 7 new tests added; 16/16 inbox tests green.

## Coordination

Zero file overlap between A and B. Merge order doesn't matter; merge as each clears review.

## Deferred (claude-native or next iteration)

- **DS-2/3/4/5** (#43, #44, #45, #46) — wait on DS-1 to land.
- **#16, #17** (OpenQuestions, ArtifactGenerator + iOS read views) — wait on DS-1 so new screens use design-system primitives.
- **#19** (OutOfSyncDetector + project-card chip) — chip lives in card that DS-3 will reskin; defer to avoid touching same area twice.
- **#20** (offline PTT/capture) — heavy iOS UI; wait on DS-3.
- **#21** (audio tiering) — pure backend, viable as a Stream C; held back to keep first v2 iteration to 2 streams.
- **#13, #14, #15** (PDF / voice-memo / URL source types) — additive but each touches the same source-ingestion pipeline; will pick up next iteration.

## How to read worker progress

Status is recorded **per-worktree**. From each worktree:

```bash
cd ../writer_os-stream-a   # or -stream-b
node "${CLAUDE_PLUGIN_ROOT:-$HOME/.claude/plugins/cache/openai-codex/codex/1.0.4}/scripts/codex-companion.mjs" status --all
```

Or via slash command in this session: `/codex:status task-<id>`. Handoffs land in `docs/handoffs/stream-<letter>-<slug>.md` when each worker exits.
