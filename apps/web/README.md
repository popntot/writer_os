# Writer OS Web Reference

Serve with `cd apps/web && python3 -m http.server 5173`, then open `http://localhost:5173/`.

This is the reference oracle for the locked design system, not the Phase-1.5 product web app. It is a disposable static playground for comparing visual intent across DS-3/4/5 reskins and future ports.
